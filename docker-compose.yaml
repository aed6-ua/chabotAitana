version: '3'
services:
  rag:
    container_name: rag
    build: .
    network_mode: host
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    ports:
      - "8100:8000"
    volumes:
      - "./app:/app"

